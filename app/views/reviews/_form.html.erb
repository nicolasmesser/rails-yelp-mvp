<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit class: "btn btn-primary" %>
  <%= link_to "Back to restaurant", restaurant_path(@restaurant), class: "btn btn-outline-primary" %>
<% end %>
